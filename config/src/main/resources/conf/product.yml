server:
  port: "8082"
  servlet:
    context-path: /api/v1
spring:
  application:
    name: product
management:
  endpoint:
    refresh:
      enabled: true
message: "Hello Product, How are you !"